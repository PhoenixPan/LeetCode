## Solution 1: accepted 64ms

```java
public int arrangeCoins(int n) {
    int row = 0;
    int cost = 1;
    while (n >= cost) {
        n -= cost;
        row++;
        cost++;
    }
    return row;
}
```
